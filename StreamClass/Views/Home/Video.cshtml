@{
    ViewData["Title"] = "Video";
}

<video controls="controls" id="video" width="500" height="500">
    <source src="@Url.Action("GetVideo", "Home", new { id = Model.Id })" type="video/mp4">
</video>
